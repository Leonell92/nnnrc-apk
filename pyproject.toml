[tool.flet]
app_name = "NNNRC Automator"
description = "Automated task completion bot for NNNRC"
org = "com.nnnrc.automator"
version = "1.0.0"
build_number = 1

[tool.flet.android]
# Permissions needed for network access + WebView
permissions = [
  "android.permission.INTERNET",
  "android.permission.ACCESS_NETWORK_STATE"
]
